This folder contains placeholder SVG images used by the demo:

- `grapes.svg` — placeholder for the grapes hero image
- `tour1.svg`, `tour2.svg`, `tour3.svg` — placeholder tour thumbnails

If you have real JPG/PNG images (for example `graps.jpg`), place them here and update the imports in `src/pages/*` to point to the actual filenames.
